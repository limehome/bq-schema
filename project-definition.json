{
  "$schema": "https://hub.dev.limehome.com/schemas/project-definition-v2.schema.json",
  "project": {
    "name": "bq-schema",
    "type": "library",
    "summary": "Library for defining and migrating BigQuery table schemas as code to keep schema and codebase in sync",
    "description": "bq-schema lets you define BigQuery table schemas as Python dataclasses, run migrations to create or update tables, and serialize and deserialize rows. It ensures schema and code stay in sync before deployment and supports versioning and bootstrapping from existing tables.",
    "packages": [
      {
        "name": "bq-schema",
        "type": "pypi"
      }
    ]
  }
}
